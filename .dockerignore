node_modules
.git
.env
data

coverage
coverage_*
*.log
*.db
*.db-wal
*.db-shm

# Not needed in Docker build
docs/
prds/
devtools/
# Except the device simulators — their images build from the repo root, so
# excluding them here makes their own COPY lines fail to resolve.
!devtools/tapo-simulator/
!devtools/sap-ocpp-simulator/
